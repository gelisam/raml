-- | Just a re-export of @Data.Yaml.Ordered@. In the future, this will be the canonical
-- name for that module\'s contents.
module Data.Yaml.Ordered.Aeson
    ( module Data.Yaml.Ordered
    ) where

import Data.Yaml.Ordered
